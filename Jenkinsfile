node {
properties([parameters([booleanParam(defaultValue: false, description: 'yes or no', name: 'make sure'), choice(choices: ['one', 'two', 'three'], description: '', name: 'make choice'), string(defaultValue: '', description: '', name: 'Name', trim: false)])])
}
